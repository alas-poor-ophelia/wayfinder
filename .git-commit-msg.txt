M11: Retire legacy e2e harness; docs reflect the plugin architecture

- Delete the symlink-based Vitest/Playwright e2e harness (tests/e2e,
  patches/, vitest.config.ts, test-vault fixtures) and its deps
  (obsidian-testing-framework, @playwright/test, patch-package).
  Visual verification is MCP/Obsidian-CLI driven; vitest remains for
  unit tests only (146 green).
- Legacy element-level reference screenshots preserved under
  references/legacy-e2e/ (fresh full-window captures in references/
  are the parity truth).
- PLAN.md replaced with a status pointer to the approved plugin plan.

Final sweep performed live: all five tabs screenshotted against the
fresh references, console clean. Out of scope by design: inventory,
XP tracker, full spellbook (mount point reserved).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
